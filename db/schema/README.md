# db/schema/

Definiciones de esquema de Drizzle ORM (tablas, relaciones, tipos).

No debe contener: migraciones generadas (van en `db/migrations/`) ni
queries o lógica de acceso a datos — solo la definición del esquema.
